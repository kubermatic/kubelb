# Copyright 2026 The KubeLB Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Default Annotations Test
# Verifies:
# - Config.spec.defaultAnnotations are applied to LB services
# - Annotations propagate to Envoy service
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: default-annotations
  labels:
    all:
    isolated: "true"
    resource: config
    feature: default-annotations
spec:
  concurrent: false
  description: |
    Test that Config.spec.defaultAnnotations are applied to created LoadBalancer services.
  steps:
    - name: reset-configs
      use:
        template: ../../../../step-templates/isolated/reset-configs.yaml
        with:
          bindings:
            - name: manifestsPath
              value: ../../../../manifests

    - name: patch-config-with-annotations
      cluster: kubelb
      try:
        - script:
            timeout: 60s
            content: |
              set -e
              kubectl patch config default -n kubelb --type=merge -p '{
                "spec": {
                  "defaultAnnotations": {
                    "service": {
                      "test.kubelb.k8c.io/annotation": "test-value",
                      "test.kubelb.k8c.io/isolated": "true"
                    }
                  }
                }
              }'
            check:
              ($error == null): true

    - name: create-loadbalancer-service
      cluster: tenant1
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: echo-annot
                namespace: default
              spec:
                type: LoadBalancer
                selector:
                  app: echo-shared
                ports:
                  - port: 80
                    targetPort: 5678
        - assert:
            timeout: 60s
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: echo-annot
                namespace: default
              status:
                loadBalancer:
                  ingress:
                    - {}

    - name: verify-annotations-on-envoy-service
      cluster: kubelb
      try:
        - script:
            timeout: 60s
            content: |
              set -e
              NAMESPACE="tenant-primary"

              for i in $(seq 1 10); do
                SVC=$(kubectl get svc -n "$NAMESPACE" -l kubelb.k8c.io/origin-name=echo-annot -o name 2>/dev/null | head -1)
                if [ -z "$SVC" ]; then
                  sleep 2
                  continue
                fi

                ANNOT=$(kubectl get $SVC -n "$NAMESPACE" -o jsonpath='{.metadata.annotations.test\.kubelb\.k8c\.io/annotation}')
                if [ "$ANNOT" = "test-value" ]; then
                  echo "Default annotation found on LoadBalancer service"
                  exit 0
                fi

                sleep 2
              done

              echo "Default annotation not found on LoadBalancer service"
              kubectl get svc -n "$NAMESPACE" -l kubelb.k8c.io/origin-name=echo-annot -o yaml
              exit 1
            check:
              ($error == null): true
      finally:
        - script:
            cluster: tenant1
            timeout: 60s
            content: |
              kubectl delete svc echo-annot -n default --ignore-not-found
        - script:
            cluster: kubelb
            timeout: 60s
            content: |
              kubectl replace -f ../../../../manifests/kubelb-manager/config.yaml
