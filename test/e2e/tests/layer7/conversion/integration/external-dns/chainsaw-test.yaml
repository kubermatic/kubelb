# Copyright 2026 The KubeLB Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: ing-conversion-integration-external-dns
  labels:
    suite: conversion
    layer: layer7
    conversion: "true"
    test: "conversion-integration-external-dns"
    category: integration
    parallel: "true"
spec:
  description: |
    Test external-dns annotation propagation.
    Verifies annotations propagated and traffic works.
  bindings:
    - name: name
      value: conv-extdns
    - name: transformed_host
      value: conv-extdns.gateway.local
  steps:
    - name: create-service
      cluster: standalone
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: conv-extdns
                namespace: default
              spec:
                type: ClusterIP
                selector:
                  app: echo-shared
                ports:
                  - port: 80
                    targetPort: 5678
      catch:
        - use:
            template: ../../../../../step-templates/debug/collect-diagnostics.yaml

    - name: create-ingress
      cluster: standalone
      try:
        - apply:
            resource:
              apiVersion: networking.k8s.io/v1
              kind: Ingress
              metadata:
                name: conv-extdns
                namespace: default
                annotations:
                  external-dns.alpha.kubernetes.io/target: "lb.example.com"
                  external-dns.alpha.kubernetes.io/hostname: "conv-extdns.example.com"
                  external-dns.alpha.kubernetes.io/ttl: "300"
              spec:
                ingressClassName: nginx
                rules:
                  - host: conv-extdns.test.local
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: conv-extdns
                              port:
                                number: 80
      catch:
        - use:
            template: ../../../../../step-templates/debug/collect-diagnostics.yaml

    - name: verify-httproute
      use:
        template: ../../../../../step-templates/conversion/verify-httproute.yaml
        with:
          bindings:
            - name: ingress_name
              value: ($name)
            - name: expected_host
              value: ($transformed_host)

    - name: verify-gateway-target
      cluster: standalone
      try:
        - script:
            timeout: 60s
            content: |
              set -e
              for i in $(seq 1 30); do
                TARGET=$(kubectl get gateway.gateway.networking.k8s.io kubelb -n default \
                  -o jsonpath='{.metadata.annotations.external-dns\.alpha\.kubernetes\.io/target}' 2>/dev/null || echo "")

                if [ "$TARGET" = "lb.example.com" ]; then
                  echo "  target: $TARGET"
                  exit 0
                fi
                sleep 2
              done
              echo "Gateway missing external-dns.alpha.kubernetes.io/target"
              exit 1
            check:
              ($error == null): true
      catch:
        - use:
            template: ../../../../../step-templates/debug/collect-diagnostics.yaml

    - name: verify-httproute-annotations
      cluster: standalone
      try:
        - script:
            timeout: 60s
            content: |
              set -e
              SOURCE_LABEL="conv-extdns.default"

              for i in $(seq 1 20); do
                HTTPROUTE=$(kubectl get httproutes.gateway.networking.k8s.io -n default \
                  -l "kubelb.k8c.io/source-ingress=${SOURCE_LABEL}" -o name 2>/dev/null | head -1)

                if [ -n "$HTTPROUTE" ]; then
                  HOSTNAME=$(kubectl get $HTTPROUTE -n default \
                    -o jsonpath='{.metadata.annotations.external-dns\.alpha\.kubernetes\.io/hostname}' 2>/dev/null || echo "")
                  TTL=$(kubectl get $HTTPROUTE -n default \
                    -o jsonpath='{.metadata.annotations.external-dns\.alpha\.kubernetes\.io/ttl}' 2>/dev/null || echo "")

                  if [ "$HOSTNAME" = "conv-extdns.example.com" ] && [ "$TTL" = "300" ]; then
                    echo "  hostname: $HOSTNAME"
                    echo "  ttl: $TTL"
                    exit 0
                  fi
                fi
                sleep 2
              done
              echo "HTTPRoute missing external-dns annotations"
              exit 1
            check:
              ($error == null): true
      catch:
        - use:
            template: ../../../../../step-templates/debug/collect-diagnostics.yaml

    - name: verify-traffic
      use:
        template: ../../../../../step-templates/conversion/verify-http-traffic.yaml
        with:
          bindings:
            - name: host
              value: ($transformed_host)
            - name: expected_response
              value: hello-shared

    - name: cleanup
      use:
        template: ../../../../../step-templates/conversion/cleanup-conversion.yaml
        with:
          bindings:
            - name: ingress_name
              value: ($name)
