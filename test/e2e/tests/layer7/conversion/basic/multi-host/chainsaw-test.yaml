# Copyright 2026 The KubeLB Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: ing-conversion-basic-multi-host
  labels:
    suite: conversion
    layer: layer7
    conversion: "true"
    test: "conversion-basic-multi-host"
    category: basic
    parallel: "true"
spec:
  description: |
    Test multi-host Ingress conversion with distinct backends per host.
    Verifies each host routes to correct backend by checking response content.
  bindings:
    - name: name
      value: conv-multihost
  steps:
    - name: deploy-backends
      cluster: standalone
      try:
        - apply:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: conv-mh-host1
                namespace: default
              spec:
                replicas: 1
                selector:
                  matchLabels:
                    app: conv-mh-host1
                template:
                  metadata:
                    labels:
                      app: conv-mh-host1
                  spec:
                    containers:
                      - name: echo
                        image: hashicorp/http-echo:1.0
                        imagePullPolicy: IfNotPresent
                        args: ["-text=response-from-host1-backend"]
                        ports:
                          - containerPort: 5678
        - apply:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: conv-mh-host2
                namespace: default
              spec:
                replicas: 1
                selector:
                  matchLabels:
                    app: conv-mh-host2
                template:
                  metadata:
                    labels:
                      app: conv-mh-host2
                  spec:
                    containers:
                      - name: echo
                        image: hashicorp/http-echo:1.0
                        imagePullPolicy: IfNotPresent
                        args: ["-text=response-from-host2-backend"]
                        ports:
                          - containerPort: 5678
        - apply:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: conv-mh-host3
                namespace: default
              spec:
                replicas: 1
                selector:
                  matchLabels:
                    app: conv-mh-host3
                template:
                  metadata:
                    labels:
                      app: conv-mh-host3
                  spec:
                    containers:
                      - name: echo
                        image: hashicorp/http-echo:1.0
                        imagePullPolicy: IfNotPresent
                        args: ["-text=response-from-host3-backend"]
                        ports:
                          - containerPort: 5678

    - name: create-services
      cluster: standalone
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: conv-mh-host1
                namespace: default
              spec:
                type: ClusterIP
                selector:
                  app: conv-mh-host1
                ports:
                  - port: 80
                    targetPort: 5678
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: conv-mh-host2
                namespace: default
              spec:
                type: ClusterIP
                selector:
                  app: conv-mh-host2
                ports:
                  - port: 80
                    targetPort: 5678
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: conv-mh-host3
                namespace: default
              spec:
                type: ClusterIP
                selector:
                  app: conv-mh-host3
                ports:
                  - port: 80
                    targetPort: 5678

    - name: wait-backends
      cluster: standalone
      try:
        - script:
            timeout: 120s
            content: |
              kubectl wait --for=condition=available deployment/conv-mh-host1 deployment/conv-mh-host2 deployment/conv-mh-host3 -n default --timeout=90s

    - name: create-ingress
      cluster: standalone
      try:
        - apply:
            resource:
              apiVersion: networking.k8s.io/v1
              kind: Ingress
              metadata:
                name: conv-multihost
                namespace: default
              spec:
                ingressClassName: nginx
                rules:
                  - host: mh-host1.test.local
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: conv-mh-host1
                              port:
                                number: 80
                  - host: mh-host2.test.local
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: conv-mh-host2
                              port:
                                number: 80
                  - host: mh-host3.test.local
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: conv-mh-host3
                              port:
                                number: 80

    - name: verify-httproute
      cluster: standalone
      try:
        - script:
            timeout: 60s
            content: |
              set -e
              SOURCE_LABEL="conv-multihost.default"

              for i in $(seq 1 30); do
                COUNT=$(kubectl get httproutes.gateway.networking.k8s.io -n default \
                  -l "kubelb.k8c.io/source-ingress=${SOURCE_LABEL}" -o name 2>/dev/null | wc -l)

                if [ "$COUNT" -ge 1 ]; then
                  kubectl get httproutes.gateway.networking.k8s.io -n default \
                    -l "kubelb.k8c.io/source-ingress=${SOURCE_LABEL}" \
                    -o custom-columns='NAME:.metadata.name,HOSTNAMES:.spec.hostnames'
                  exit 0
                fi

                sleep 2
              done

              echo "No HTTPRoute found"
              exit 1
            check:
              ($error == null): true

    - name: verify-status
      use:
        template: ../../../../../step-templates/conversion/verify-status-annotation.yaml
        with:
          bindings:
            - name: ingress_name
              value: ($name)
            - name: expected_status
              value: converted

    - name: verify-http-host1
      use:
        template: ../../../../../step-templates/conversion/verify-http-traffic.yaml
        with:
          bindings:
            - name: host
              value: mh-host1.gateway.local
            - name: expected_response
              value: response-from-host1-backend

    - name: verify-http-host2
      use:
        template: ../../../../../step-templates/conversion/verify-http-traffic.yaml
        with:
          bindings:
            - name: host
              value: mh-host2.gateway.local
            - name: expected_response
              value: response-from-host2-backend

    - name: verify-http-host3
      use:
        template: ../../../../../step-templates/conversion/verify-http-traffic.yaml
        with:
          bindings:
            - name: host
              value: mh-host3.gateway.local
            - name: expected_response
              value: response-from-host3-backend

    - name: cleanup
      cluster: standalone
      try:
        - script:
            timeout: 30s
            content: |
              kubectl delete ingress conv-multihost -n default --ignore-not-found
              kubectl delete service conv-mh-host1 conv-mh-host2 conv-mh-host3 -n default --ignore-not-found
              kubectl delete deployment conv-mh-host1 conv-mh-host2 conv-mh-host3 -n default --ignore-not-found
              kubectl delete httproutes.gateway.networking.k8s.io -n default \
                -l "kubelb.k8c.io/source-ingress=conv-multihost.default" --ignore-not-found
