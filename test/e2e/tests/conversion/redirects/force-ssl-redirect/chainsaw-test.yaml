# Copyright 2026 The KubeLB Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: conversion-redirects-force-ssl
  labels:
    conversion: "true"
    test: "conversion-redirects-force-ssl"
    category: redirects
    parallel: "true"
spec:
  description: |
    Test force-ssl-redirect annotation conversion.
    Verifies HTTP requests get redirect to HTTPS.
  bindings:
    - name: name
      value: conv-force-ssl
    - name: transformed_host
      value: conv-forcessl.gateway.local
  steps:
    - name: create-service
      description: Create ClusterIP service
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: conv-force-ssl
                namespace: default
              spec:
                type: ClusterIP
                selector:
                  app: echo-shared
                ports:
                  - port: 80
                    targetPort: 5678

    - name: create-ingress
      description: Create Ingress with force-ssl-redirect=true
      try:
        - apply:
            resource:
              apiVersion: networking.k8s.io/v1
              kind: Ingress
              metadata:
                name: conv-force-ssl
                namespace: default
                annotations:
                  nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
              spec:
                ingressClassName: nginx
                rules:
                  - host: conv-forcessl.test.local
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: conv-force-ssl
                              port:
                                number: 80

    - name: verify-httproute
      description: Verify HTTPRoute exists with redirect filter
      try:
        - script:
            timeout: 60s
            content: |
              set -e
              SOURCE_LABEL="conv-force-ssl.default"

              for i in $(seq 1 30); do
                HTTPROUTE=$(kubectl get httproutes.gateway.networking.k8s.io -n default \
                  -l "kubelb.k8c.io/source-ingress=${SOURCE_LABEL}" -o name 2>/dev/null | head -1)

                if [ -n "$HTTPROUTE" ]; then
                  FILTERS=$(kubectl get $HTTPROUTE -n default -o jsonpath='{.spec.rules[*].filters}')
                  if echo "$FILTERS" | grep -q "RequestRedirect"; then
                    echo "SUCCESS: HTTPRoute has RequestRedirect filter"
                    exit 0
                  fi
                fi
                sleep 2
              done
              echo "FAILED: No redirect filter found"
              exit 1
            check:
              ($error == null): true

    - name: verify-redirect-traffic
      description: Verify actual HTTP→HTTPS redirect
      use:
        template: ../../../../step-templates/conversion/verify-redirect.yaml
        with:
          bindings:
            - name: host
              value: ($transformed_host)
            - name: expected_status
              value: "301"
            - name: expected_location
              value: "https://"

    - name: cleanup
      description: Cleanup resources
      use:
        template: ../../../../step-templates/conversion/cleanup-conversion.yaml
        with:
          bindings:
            - name: ingress_name
              value: ($name)
