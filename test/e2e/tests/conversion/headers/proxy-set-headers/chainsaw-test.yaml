# Copyright 2026 The KubeLB Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Proxy Set Headers Conversion Test
# Verifies:
# - nginx.ingress.kubernetes.io/proxy-set-headers annotation
# - HTTPRoute has RequestHeaderModifier filter
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: conversion-headers-proxy-set
  labels:
    conversion: "true"
    category: headers
    parallel: "true"
spec:
  description: |
    Test proxy-set-headers annotation conversion.
    Note: ConfigMap reference generates warning, inline values converted.
  bindings:
    - name: name
      value: conv-proxy-headers
    - name: namespace
      value: default
    - name: transformed_host
      value: conv-proxyheaders.gateway.local
  steps:
    - name: create-service
      description: Create ClusterIP service
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Service
              metadata:
                name: conv-proxy-headers
                namespace: default
              spec:
                type: ClusterIP
                selector:
                  app: echo-shared
                ports:
                  - port: 80
                    targetPort: 5678

    - name: create-configmap
      description: Create ConfigMap with headers
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: conv-proxy-headers
                namespace: default
              data:
                X-Custom-Header: "custom-value"
                X-Forwarded-Proto: "https"

    - name: create-ingress
      description: Create Ingress with proxy-set-headers
      try:
        - apply:
            resource:
              apiVersion: networking.k8s.io/v1
              kind: Ingress
              metadata:
                name: conv-proxy-headers
                namespace: default
                annotations:
                  nginx.ingress.kubernetes.io/proxy-set-headers: default/conv-proxy-headers
              spec:
                ingressClassName: nginx
                rules:
                  - host: conv-proxyheaders.test.local
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: conv-proxy-headers
                              port:
                                number: 80

    - name: verify-httproute
      description: Verify HTTPRoute exists
      use:
        template: ../../../../step-templates/conversion/verify-httproute.yaml
        with:
          bindings:
            - name: ingress_name
              value: ($name)
            - name: namespace
              value: ($namespace)
            - name: expected_host
              value: ($transformed_host)

    - name: verify-status
      description: Verify conversion status
      try:
        - script:
            timeout: 30s
            content: |
              set -e
              STATUS=$(kubectl get ingress conv-proxy-headers -n default \
                -o jsonpath='{.metadata.annotations.kubelb\.k8c\.io/conversion-status}' 2>/dev/null || echo "")
              if [ "$STATUS" = "converted" ] || [ "$STATUS" = "partial" ]; then
                echo "SUCCESS: Status is '$STATUS'"
                exit 0
              fi
              echo "FAILED: Expected status 'converted' or 'partial', got '$STATUS'"
              exit 1
            check:
              ($error == null): true

    - name: cleanup
      description: Cleanup resources
      try:
        - script:
            timeout: 30s
            content: |
              kubectl delete ingress conv-proxy-headers -n default --ignore-not-found
              kubectl delete service conv-proxy-headers -n default --ignore-not-found
              kubectl delete configmap conv-proxy-headers -n default --ignore-not-found
              kubectl delete httproutes.gateway.networking.k8s.io -n default \
                -l "kubelb.k8c.io/source-ingress=conv-proxy-headers.default" --ignore-not-found
