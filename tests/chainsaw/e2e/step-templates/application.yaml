apiVersion: chainsaw.kyverno.io/v1alpha1
kind: StepTemplate
metadata:
  name: deploy-application
spec:
  try:
    - apply:
        file: ../../testdata/apps/nginx-deployment.yaml
    - assert:
        file: ../../testdata/assert/deployment-ready.yaml
